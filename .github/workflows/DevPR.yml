name: Test 🧪 Scan 🔍 and Build 🔨 for Dev

on:
  pull_request:
    branches:
      - Dev
        
env:
  UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
  
jobs:
  callTo-Labeler:
    uses: VoroninLevan/EDA/.github/workflows/Labeler.yml
  callTo-TestWF:
    uses: VoroninLevan/EDA/.github/workflows/TestWF.yml
  callTo-BuildAndroid:
    uses: VoroninLevan/EDA/.github/workflows/BuildAndroid.yml
